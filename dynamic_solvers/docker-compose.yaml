name: oop
services:
  benchmarker:
#    image: localhost/benchmark-z3-15.0
    build:
      dockerfile: dynamic_solvers/Dockerfile
      context: ..
    container_name: oop
    environment:
      - CONFIG_CSV=/configs/Pos-POP-config.csv
      - OUTPUT_CSV=/out/results.csv
      - VERBOSE=-v
    env_file: .env
    volumes:
      - ./configs:/configs
      - ./out:/out
